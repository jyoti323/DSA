rows=3
cols=4
firstMatrix=[]

for i in range(rows):
    rowArray=[]
    for j in range(cols):
        rowArray.append(int(input("Enter the value of the matrix: ")))
    firstMatrix.append(rowArray)

print(firstMatrix)


#traversal of matrix
